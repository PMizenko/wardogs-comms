# Normalise to LF in the repository; Git checks out whatever the platform
# wants. Without this, a Windows clone rewrites every file and the first diff
# on a Mac or CI box is the entire tree.
* text=auto eol=lf

*.png binary
*.ico binary
*.exe binary

# Generated; collapse it in diffs and reviews.
package-lock.json linguist-generated=true
